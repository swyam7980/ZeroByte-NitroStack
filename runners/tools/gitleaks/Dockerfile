# gitleaks sandbox image (§8, §2.4). Static scan over a mounted repo path.
# Redaction is applied by the Secrets MCP service before anything leaves.
FROM zricethezav/gitleaks:latest

USER 1000
ENTRYPOINT ["gitleaks"]
